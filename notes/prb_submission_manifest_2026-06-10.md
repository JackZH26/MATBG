# PRB Submission Manifest Note

Date: 2026-06-10

The active manifest is:

```text
data/processed/prb_submission_manifest.csv
```

It is generated by:

```bash
python3 scripts/build_prb_submission_manifest.py
```

The 2026-06-10 manifest extends the earlier checkpoint by requiring:

1. the point-by-point response to reviewers,
2. all YAML configuration files,
3. the pairing-family response data, summary, and audit,
4. the grid/truncation/shell robustness matrix, summary, and audit,
5. the valley-partner response-sensitivity data, summary, and audit,
6. the 2026-06-10 figure/table provenance and readiness notes.

All required manifest rows must have `status=present` before the local package
builder is allowed to proceed.
